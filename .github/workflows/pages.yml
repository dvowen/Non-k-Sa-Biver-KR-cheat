name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify
        run: |
          node --test scripts/translation-utils.test.mjs scripts/apply-translations.test.mjs scripts/debug-tools-utils.test.mjs scripts/generate-debug-items.test.mjs scripts/inject-debug-tools.test.mjs scripts/build-github-pages.test.mjs scripts/cheat-build-policy.test.mjs
          node --check scripts/build-github-pages.mjs

      - name: Build
        run: |
          node scripts/merge-translations.mjs
          node scripts/apply-translations.mjs
          node scripts/build-github-pages.mjs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
